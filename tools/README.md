# 标定与点云工具

- `multi_frame_sphere_fit`：将同一球靶的多帧 PCD 对齐、裁剪并鲁棒拟合一个高精度球心。适用于单球标定与球心质量检查，需要 PCL。
- `sphere_target_registration`：拟合三个或更多球靶，并求出累计点云到厂区/世界坐标系的 6 自由度刚体变换。只依赖 NumPy。

两者互补：前者解决“一个球心怎样拟合得稳定”，后者解决“多个球心怎样定义世界坐标”。它们都不是覆盖规划器的运行时依赖。
